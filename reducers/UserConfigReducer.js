export default function UserConfigReducer(state = {
                                                  username: ''
                                                },
                                                action) {

  switch (action.type) {
  
      default:
      return state
  }

  return state
}
